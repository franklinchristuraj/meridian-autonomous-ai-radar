---
phase: 02-scout-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/__init__.py
  - src/pipeline/scout.py
  - src/db/schema.py
  - tests/test_scout.py
  - requirements.txt
autonomous: true
requirements: [INGEST-01, INGEST-02, INTEL-01]

must_haves:
  truths:
    - "fetch_arxiv_papers returns a list of arxiv Results for a given date"
    - "keyword_filter returns at most 50 papers ranked by keyword density from live pattern keywords"
    - "get_top_patterns returns top 5 patterns with name, uuid, distance from nearVector search"
    - "score_paper calls Haiku with paper + 5 patterns and parses structured JSON response"
    - "score_paper handles malformed Haiku JSON gracefully via regex fallback"
    - "arxiv_id is normalized to strip URL prefix and version suffix"
    - "write_signal skips papers whose arxiv_id already exists in Weaviate"
    - "Tier assignment is correct: >= 7.0 BRIEF, >= 5.0 VAULT, < 5.0 ARCHIVE"
    - "Signals schema includes reasoning TEXT property"
  artifacts:
    - path: "src/pipeline/__init__.py"
      provides: "Module init"
    - path: "src/pipeline/scout.py"
      provides: "All Scout helper functions"
      exports: ["fetch_arxiv_papers", "build_arxiv_query", "fetch_pattern_keywords", "keyword_filter", "keyword_density", "get_top_patterns", "score_paper", "normalize_arxiv_id", "arxiv_id_exists", "write_signal", "assign_tier"]
    - path: "tests/test_scout.py"
      provides: "Unit tests for all Scout helpers"
      min_lines: 100
    - path: "src/db/schema.py"
      provides: "Signals collection with reasoning property"
      contains: "reasoning"
  key_links:
    - from: "src/pipeline/scout.py"
      to: "src/runtime/claude_runner.py"
      via: "invoke_claude(prompt, model='claude-haiku-4-5')"
      pattern: "invoke_claude.*haiku"
    - from: "src/pipeline/scout.py"
      to: "src/db/client.py"
      via: "get_client() for Weaviate operations"
      pattern: "from src\\.db\\.client import"
    - from: "src/pipeline/scout.py"
      to: "Weaviate Patterns collection"
      via: "near_text query for pattern matching"
      pattern: "near_text"
    - from: "src/pipeline/scout.py"
      to: "Weaviate Signals collection"
      via: "filter + insert for dedup write"
      pattern: "Filter\\.by_property.*arxiv_id"
---

<objective>
Implement all Scout pipeline helper functions and their unit tests, plus the schema migration to add the `reasoning` field to the Signals collection.

Purpose: These are the building blocks of the daily Scout pipeline. Each function is independently testable and has clear I/O contracts. Plan 02 will wire them into the orchestrator.
Output: `src/pipeline/scout.py` with all helpers, `tests/test_scout.py` with comprehensive unit tests, updated schema and requirements.txt.
</objective>

<execution_context>
@/Users/a.christuraj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a.christuraj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scout-pipeline/02-CONTEXT.md
@.planning/phases/02-scout-pipeline/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/runtime/claude_runner.py:
```python
def invoke_claude(
    prompt: str,
    model: str = "claude-sonnet-4-5",
    timeout: int = 120,
) -> dict:
    """Returns parsed JSON dict with keys: result, model, usage, cost_usd.
    Raises RuntimeError on non-zero exit, json.JSONDecodeError on bad output."""
```

From src/db/client.py:
```python
def get_client() -> weaviate.WeaviateClient:
    """Return a connected Weaviate client using env-configured host/ports."""
```

From src/db/schema.py (Signals collection properties):
```python
# Existing properties: source_url, title, abstract, published_date, score, tier, status, arxiv_id, matched_pattern_ids
# MISSING: reasoning (TEXT) — must be added by this plan
```

From src/db/schema.py (Patterns collection properties):
```python
# Properties: name, description, keywords (TEXT_ARRAY), maturity, contrarian_take, related_patterns, vault_source, example_signals
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema migration, dependency, and Scout helper functions with tests</name>
  <files>src/db/schema.py, requirements.txt, src/pipeline/__init__.py, src/pipeline/scout.py, tests/test_scout.py</files>
  <behavior>
    - test_build_arxiv_query: builds correct query string with 6 categories and submittedDate wildcard for a given date
    - test_fetch_arxiv_papers: mocks arxiv.Client and returns list of Result-like objects for a date
    - test_normalize_arxiv_id: "http://arxiv.org/abs/2401.12345v1" -> "2401.12345", "http://arxiv.org/abs/2401.12345" -> "2401.12345"
    - test_fetch_pattern_keywords: mocks Weaviate Patterns collection, returns deduplicated lowercase keyword list
    - test_keyword_density: "agent orchestration framework" with keywords ["agent", "orchestration"] -> 2
    - test_keyword_filter_cap: 100 papers in, cap=50, returns top 50 by density; papers with 0 hits excluded
    - test_keyword_filter_excludes_zero_hits: papers with no keyword matches are excluded even under cap
    - test_get_top_patterns: mocks Weaviate near_text, returns top 5 dicts with name, description, contrarian_take, keywords, uuid, distance
    - test_score_paper: mocks invoke_claude returning valid JSON, parses score + matched_pattern_names + reasoning
    - test_score_paper_bad_json: mocks invoke_claude returning prose-wrapped JSON, regex fallback extracts it
    - test_score_paper_unparseable: mocks invoke_claude returning garbage, raises ValueError
    - test_assign_tier: 7.0 -> BRIEF, 7.5 -> BRIEF, 6.9 -> VAULT, 5.0 -> VAULT, 4.9 -> ARCHIVE, 1.0 -> ARCHIVE
    - test_arxiv_id_exists_true: mocks Weaviate filter returning 1 object -> True
    - test_arxiv_id_exists_false: mocks Weaviate filter returning 0 objects -> False
    - test_write_signal_new: mocks arxiv_id_exists=False, verifies insert called with correct data dict
    - test_write_signal_duplicate: mocks arxiv_id_exists=True, verifies insert NOT called
  </behavior>
  <action>
  **Step 0 — Prerequisites:**
  1. Add `arxiv>=2.4,<3` to `requirements.txt`
  2. Add `reasoning` TEXT property to Signals collection in `src/db/schema.py`. Use idempotent migration: after the `_create_signals` function, add a helper `_migrate_signals_reasoning(client)` that checks if the property exists and adds it if not. Call it from `init_schema()`. Use `collection.config.add_property(Property(name="reasoning", data_type=DataType.TEXT))` wrapped in a try/except for the case where it already exists.
  3. Create `src/pipeline/__init__.py` (empty)

  **Step 1 — Write tests first (RED):**
  Create `tests/test_scout.py` with all behaviors listed above. Use `unittest.mock.patch` and `unittest.mock.MagicMock` to mock:
  - `arxiv.Client` and its `.results()` method (return list of mock Result objects with entry_id, title, summary, published, primary_category)
  - `weaviate.WeaviateClient` collections for near_text, fetch_objects, filter queries
  - `invoke_claude` from `src.runtime.claude_runner`

  Follow the established mock patterns from `tests/test_claude_runner.py`.

  Run tests — they MUST fail (no implementation yet).

  **Step 2 — Implement helpers (GREEN):**
  Create `src/pipeline/scout.py` with these functions:

  - `build_arxiv_query(target_date: date) -> str` — combines 6 `cat:` filters with OR + `submittedDate:{YYYYMMDD}*`
  - `fetch_arxiv_papers(target_date: date | None = None) -> list` — uses `arxiv.Client(num_retries=3, page_size=100)` with `arxiv.Search(query=..., max_results=500, sort_by=SubmittedDate, sort_order=Descending)`. Defaults to yesterday.
  - `normalize_arxiv_id(entry_id: str) -> str` — `entry_id.split("/abs/")[-1].split("v")[0]`
  - `fetch_pattern_keywords(client: WeaviateClient) -> list[str]` — fetch all Patterns, collect keywords, deduplicate, lowercase
  - `keyword_density(text: str, keywords: list[str]) -> int` — count keyword hits in lowercased text
  - `keyword_filter(papers: list, keywords: list[str], cap: int = 50) -> list` — score by density, exclude 0-hit, sort descending, take top cap
  - `get_top_patterns(client: WeaviateClient, title: str, abstract: str, top_n: int = 5) -> list[dict]` — near_text on Patterns collection with `f"{title}\n\n{abstract}"`, return dicts with name, description, contrarian_take, keywords, uuid, distance
  - `score_paper(title: str, abstract: str, patterns: list[dict]) -> dict` — build scoring prompt per RESEARCH.md Pattern 3, call `invoke_claude(prompt, model="claude-haiku-4-5")`, parse JSON from `raw["result"]` with regex fallback `re.search(r'\{.*\}', raw["result"], re.DOTALL)`. Raise ValueError if unparseable.
  - `assign_tier(score: float) -> str` — BRIEF if >= 7.0, VAULT if >= 5.0, else ARCHIVE
  - `arxiv_id_exists(client: WeaviateClient, arxiv_id: str) -> bool` — filter query on Signals by arxiv_id, return len > 0
  - `write_signal(client: WeaviateClient, data: dict) -> None` — check arxiv_id_exists, skip if True, else insert

  The scoring prompt MUST include pattern contrarian_take fields per locked decision. Use the exact prompt template from RESEARCH.md Pattern 3.

  **Step 3 — Run tests (all must pass).**

  **Step 4 — Refactor if needed:** Extract constants (CATEGORIES list, SCORING_PROMPT_TEMPLATE) to module level. Ensure logging with `logging.getLogger(__name__)`.
  </action>
  <verify>
    <automated>cd /Users/a.christuraj/Projects/meridian && pip install "arxiv>=2.4,<3" -q && pytest tests/test_scout.py -x -q</automated>
  </verify>
  <done>All 16+ tests pass. Schema migration adds reasoning property idempotently. All helper functions have clear I/O and are independently importable from src.pipeline.scout.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_scout.py -x -q` — all tests pass
- `pytest tests/ -q` — full suite still green (no regressions)
- `grep "reasoning" src/db/schema.py` — property exists in schema
- `grep "arxiv" requirements.txt` — dependency listed
- `python -c "from src.pipeline.scout import fetch_arxiv_papers, keyword_filter, score_paper, get_top_patterns, write_signal, assign_tier"` — all imports work
</verification>

<success_criteria>
All Scout helper functions implemented with unit tests. Schema migrated. arxiv dependency added. Every function independently testable and importable. Tests cover happy path, edge cases (bad JSON, duplicates, zero-hit filtering), and tier boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/02-scout-pipeline/02-01-SUMMARY.md`
</output>
