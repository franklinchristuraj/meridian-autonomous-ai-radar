---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/__init__.py
  - src/api/main.py
  - src/api/auth.py
  - src/api/routes/__init__.py
  - src/api/routes/trigger.py
  - src/runtime/__init__.py
  - src/runtime/claude_runner.py
  - tests/test_trigger.py
  - tests/test_claude_runner.py
autonomous: true
requirements:
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "POST /pipeline/trigger with valid X-API-Key returns 202 Accepted"
    - "POST /pipeline/trigger with invalid or missing key returns 403 Forbidden"
    - "Claude Code CLI subprocess wrapper parses JSON output correctly"
  artifacts:
    - path: "src/api/main.py"
      provides: "FastAPI application with lifespan and router"
      contains: "FastAPI"
    - path: "src/api/auth.py"
      provides: "API key validation dependency"
      exports: ["verify_api_key"]
    - path: "src/api/routes/trigger.py"
      provides: "Pipeline trigger endpoint returning 202"
      contains: "status_code=202"
    - path: "src/runtime/claude_runner.py"
      provides: "Claude Code CLI subprocess wrapper"
      exports: ["invoke_claude"]
    - path: "tests/test_trigger.py"
      provides: "Trigger endpoint unit tests"
      min_lines: 25
    - path: "tests/test_claude_runner.py"
      provides: "Claude CLI smoke test"
      min_lines: 15
  key_links:
    - from: "src/api/routes/trigger.py"
      to: "src/api/auth.py"
      via: "Security dependency injection"
      pattern: "Security\\(verify_api_key\\)"
    - from: "src/api/main.py"
      to: "src/api/routes/trigger.py"
      via: "router include"
      pattern: "include_router"
    - from: "tests/test_trigger.py"
      to: "src/api/main.py"
      via: "FastAPI TestClient"
      pattern: "TestClient"
---

<objective>
Build the FastAPI pipeline orchestrator with webhook trigger endpoint (shared-secret auth) and the Claude Code CLI subprocess wrapper.

Purpose: The trigger endpoint is how Make.com (and manual webhooks) kick off the pipeline. The Claude runner is how the pipeline invokes LLM agents. Both are required before Phase 2 Scout can run.
Output: Working FastAPI app with authenticated trigger endpoint, Claude CLI wrapper, and passing tests for both.
</objective>

<execution_context>
@/Users/a.christuraj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a.christuraj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FastAPI trigger endpoint with shared-secret auth</name>
  <files>src/api/__init__.py, src/api/main.py, src/api/auth.py, src/api/routes/__init__.py, src/api/routes/trigger.py, tests/test_trigger.py</files>
  <behavior>
    - test_trigger_returns_202: POST /pipeline/trigger with header X-API-Key matching X_API_KEY env var returns 202 and body {"status": "accepted"}
    - test_trigger_rejects_bad_key: POST /pipeline/trigger with wrong X-API-Key returns 403
    - test_trigger_rejects_missing_key: POST /pipeline/trigger without X-API-Key header returns 403
  </behavior>
  <action>
1. Create `src/api/auth.py`:
   - Import `APIKeyHeader` and `Security` from `fastapi.security`.
   - `api_key_header = APIKeyHeader(name="X-API-Key", auto_error=True)`
   - `async def verify_api_key(key: str = Security(api_key_header)) -> str` — compares against `os.environ["X_API_KEY"]`, raises HTTPException 403 if mismatch.

2. Create `src/api/routes/trigger.py`:
   - `router = APIRouter(prefix="/pipeline", tags=["pipeline"])`
   - `@router.post("/trigger", status_code=202)` with `BackgroundTasks` and `Security(verify_api_key)` dependency.
   - Background task calls a stub `run_scout_pipeline()` (logs "Pipeline triggered — stub for Phase 1").
   - Returns `{"status": "accepted"}`.

3. Create `src/api/main.py`:
   - `app = FastAPI(title="Meridian Pipeline", version="0.1.0")`
   - Include trigger router.
   - Add a health check `GET /health` returning `{"status": "ok"}`.

4. Create `tests/test_trigger.py`:
   - Write tests FIRST. Use `httpx.ASGITransport` + `httpx.AsyncClient` (or `from starlette.testclient import TestClient`).
   - Set `X_API_KEY=test-secret` in test environment via monkeypatch or conftest fixture.
   - Three tests as described in behavior block.

5. Create empty `__init__.py` files for `src/api/`, `src/api/routes/`, `src/runtime/`.

Use `python-dotenv` to load `.env` in main.py. Make.com scope is dumb trigger only — all logic stays in FastAPI per user decision.
  </action>
  <verify>
    <automated>cd /Users/a.christuraj/Projects/meridian && pytest tests/test_trigger.py -v -x</automated>
  </verify>
  <done>FastAPI trigger endpoint returns 202 with valid key, 403 with invalid/missing key. Three tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Claude Code CLI subprocess wrapper</name>
  <files>src/runtime/claude_runner.py, tests/test_claude_runner.py</files>
  <behavior>
    - test_invoke_claude_returns_parsed_json: Mocked subprocess returns valid JSON with "result" key; invoke_claude parses it correctly
    - test_invoke_claude_raises_on_nonzero_exit: Mocked subprocess returns returncode=1; invoke_claude raises RuntimeError
    - test_invoke_claude_raises_on_invalid_json: Mocked subprocess returns non-JSON stdout; invoke_claude raises json.JSONDecodeError or ValueError
  </behavior>
  <action>
1. Create `src/runtime/claude_runner.py`:
   - `def invoke_claude(prompt: str, model: str = "claude-sonnet-4-5", timeout: int = 120) -> dict`
   - Uses `subprocess.run(["claude", "-p", "--output-format", "json", "--model", model], input=prompt, capture_output=True, text=True, timeout=timeout)`
   - Raises `RuntimeError` on non-zero returncode (include stderr in message).
   - Returns `json.loads(result.stdout)`.
   - Expected output structure: `{"result": "...", "model": "...", "usage": {...}, "cost_usd": ...}`

2. Create `tests/test_claude_runner.py`:
   - Write tests FIRST. Use `unittest.mock.patch("subprocess.run")` to mock the CLI.
   - Mock successful response: `CompletedProcess(returncode=0, stdout='{"result":"test","model":"claude-sonnet-4-5","usage":{"input_tokens":10,"output_tokens":20},"cost_usd":0.001}', stderr="")`
   - Mock failure: `CompletedProcess(returncode=1, stdout="", stderr="error message")`
   - Mock invalid JSON: `CompletedProcess(returncode=0, stdout="not json", stderr="")`
   - Three tests as described in behavior block.

NOTE: These are unit tests with mocked subprocess — they do NOT require Claude CLI to be installed locally. The VPS smoke test (actual CLI invocation) is a manual verification item per the validation strategy.
  </action>
  <verify>
    <automated>cd /Users/a.christuraj/Projects/meridian && pytest tests/test_claude_runner.py -v -x</automated>
  </verify>
  <done>Claude runner wrapper correctly parses JSON output, raises on errors. Three tests pass with mocked subprocess. Module ready for Phase 2 agent invocations.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_trigger.py tests/test_claude_runner.py -v` shows 6 tests passing
- FastAPI app starts without errors: `python -c "from src.api.main import app; print(app.title)"`
- No hardcoded secrets in source code (all from env vars)
</verification>

<success_criteria>
1. POST /pipeline/trigger with valid X-API-Key returns 202 with {"status": "accepted"}
2. POST /pipeline/trigger with invalid/missing key returns 403
3. Claude runner parses valid JSON, raises on errors, handles timeouts
4. All 6 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
