---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pytest.ini
  - requirements.txt
  - .env.example
  - src/__init__.py
  - src/db/__init__.py
  - src/db/client.py
  - src/db/schema.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_schema.py
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "All five Weaviate collections (Signals, Patterns, Hypotheses, Feedback, Briefings) exist after schema init"
    - "Inserting and querying 10 synthetic objects per collection succeeds without error"
    - "Collections use PascalCase names and text2vec-ollama vectorizer with nomic-embed-text"
  artifacts:
    - path: "src/db/schema.py"
      provides: "Collection definitions for all 5 Weaviate collections"
      contains: "Signals.*Patterns.*Hypotheses.*Feedback.*Briefings"
    - path: "src/db/client.py"
      provides: "Weaviate connection factory"
      exports: ["get_client"]
    - path: "tests/test_schema.py"
      provides: "Round-trip integration tests for all 5 collections"
      min_lines: 40
  key_links:
    - from: "src/db/schema.py"
      to: "src/db/client.py"
      via: "get_client() connection"
      pattern: "get_client"
    - from: "tests/test_schema.py"
      to: "src/db/schema.py"
      via: "schema init function import"
      pattern: "from src.db.schema import"
---

<objective>
Set up the Meridian project skeleton, install dependencies, deploy all five Weaviate collections with locked schema, and validate with round-trip tests.

Purpose: Weaviate is the data backbone for the entire system. Every other component (patterns, signals, briefings) depends on these collections existing with correct schema. This must land first.
Output: Working project with pytest infrastructure, Weaviate client, 5 collections deployed, passing integration tests.
</objective>

<execution_context>
@/Users/a.christuraj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a.christuraj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project skeleton, dependencies, and test infrastructure</name>
  <files>pytest.ini, requirements.txt, .env.example, src/__init__.py, src/db/__init__.py, tests/__init__.py, tests/conftest.py</files>
  <action>
Create the Meridian project structure:

1. Create `requirements.txt` with pinned dependencies:
   ```
   weaviate-client>=4.9,<5
   fastapi>=0.115,<1
   uvicorn[standard]>=0.30,<1
   python-dotenv>=1.0,<2
   httpx>=0.27,<1
   pydantic>=2,<3
   pytest>=8,<9
   pytest-asyncio>=0.23,<1
   ```

2. Create `pytest.ini`:
   ```ini
   [pytest]
   testpaths = tests
   asyncio_mode = auto
   ```

3. Create `.env.example` with all required env vars:
   ```
   WEAVIATE_HOST=localhost
   WEAVIATE_HTTP_PORT=8080
   WEAVIATE_GRPC_PORT=50051
   ANTHROPIC_API_KEY=sk-ant-...
   X_API_KEY=your-shared-secret-here
   OLLAMA_API_ENDPOINT=http://localhost:11434
   ```

4. Create `src/__init__.py`, `src/db/__init__.py`, `tests/__init__.py` as empty init files.

5. Create `tests/conftest.py` with:
   - A `weaviate_client` fixture that calls `get_client()` from `src.db.client`, yields the client, then closes it.
   - An `init_schema` fixture (session-scoped, autouse=False) that calls the schema init function to ensure collections exist before tests run.
  </action>
  <verify>
    <automated>cd /Users/a.christuraj/Projects/meridian && pip install -r requirements.txt -q && pytest --version</automated>
  </verify>
  <done>Project skeleton exists, all dependencies installed, pytest runs without import errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Weaviate schema — 5 collections with locked definitions and round-trip tests</name>
  <files>src/db/client.py, src/db/schema.py, tests/test_schema.py</files>
  <behavior>
    - test_collections_exist: After schema init, all 5 collection names (Signals, Patterns, Hypotheses, Feedback, Briefings) exist in Weaviate
    - test_round_trip_signals: Insert 10 synthetic Signal objects, query back, count == 10
    - test_round_trip_patterns: Insert 10 synthetic Pattern objects, query back, count == 10
    - test_round_trip_hypotheses: Insert 10 synthetic Hypothesis objects, query back, count == 10
    - test_round_trip_feedback: Insert 10 synthetic Feedback objects, query back, count == 10
    - test_round_trip_briefings: Insert 10 synthetic Briefing objects, query back, count == 10
  </behavior>
  <action>
1. Create `src/db/client.py`:
   - `get_client()` function using `weaviate.connect_to_local()` with host/port from env vars (default localhost:8080, gRPC 50051).
   - Uses `python-dotenv` to load `.env` file.

2. Create `src/db/schema.py`:
   - `init_schema(client)` function that creates all 5 collections if they don't exist (idempotent via `client.collections.exists()` check).
   - Use `vector_config=Configure.Vectors.text2vec_ollama()` with `api_endpoint` from OLLAMA_API_ENDPOINT env var, model `nomic-embed-text`.
   - Use `vector_index_config=Configure.VectorIndex.hnsw(quantizer=Configure.VectorIndex.Quantizer.rq(compression_level=8))` for RQ8 quantization.
   - All collection names PascalCase per Weaviate convention.

   Collection property definitions (from RESEARCH.md):
   - **Signals**: source_url (TEXT), title (TEXT), abstract (TEXT), published_date (DATE), score (NUMBER), tier (TEXT), status (TEXT — pending/scored/archived), arxiv_id (TEXT), matched_pattern_ids (TEXT_ARRAY)
   - **Patterns**: name (TEXT), description (TEXT), keywords (TEXT_ARRAY), maturity (TEXT — emerging/established/declining), contrarian_take (TEXT), related_patterns (TEXT_ARRAY), vault_source (TEXT), example_signals (TEXT_ARRAY)
   - **Hypotheses**: statement (TEXT), confidence (NUMBER), evidence_signal_ids (TEXT_ARRAY), created_date (DATE), status (TEXT)
   - **Feedback**: signal_id (TEXT), pattern_id (TEXT), rating (INT), comment (TEXT), created_date (DATE)
   - **Briefings**: date (DATE), summary (TEXT), generated_at (DATE), item_count (INT), items_json (TEXT — serialized JSON array of signal summaries)

3. Create `tests/test_schema.py`:
   - Write tests FIRST (RED phase), then implement to make them pass (GREEN).
   - Use `weaviate_client` and `init_schema` fixtures from conftest.
   - `test_collections_exist`: Assert all 5 names present.
   - 5 round-trip tests: Each inserts 10 synthetic objects with realistic fake data, queries back with `collection.query.fetch_objects(limit=10)`, asserts count == 10.
   - Cleanup: Each round-trip test deletes its synthetic objects after assertion (use `collection.data.delete_many` or iterate delete by UUID).

IMPORTANT: Use `Configure.Vectors` (NOT `Configure.NamedVectors` — deprecated in v4.16+). Use `vector_config=` (NOT `vectorizer_config=`).
  </action>
  <verify>
    <automated>cd /Users/a.christuraj/Projects/meridian && pytest tests/test_schema.py -v -x</automated>
  </verify>
  <done>All 5 collections deployed in Weaviate with correct properties. 6 tests pass: 1 existence check + 5 round-trip tests (10 objects each). Schema is locked and idempotent.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_schema.py -v` shows 6 tests passing
- Weaviate collections persist across test runs (idempotent schema init)
- No deprecated v4.16 API usage (no `NamedVectors`, no `vectorizer_config`)
</verification>

<success_criteria>
1. Five Weaviate collections exist with correct properties and vector config
2. Round-trip insert+query of 10 objects per collection passes without error
3. Schema init is idempotent (re-running does not error or duplicate)
4. Test infrastructure (pytest + conftest) works for all subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
